# AegisAI - Smart City Risk Intelligence System
# Python 3.10+ required

# Phase 1: Perception Layer
ultralytics>=8.0.0
supervision>=0.18.0          # ByteTrack tracker (CPU-friendly)
opencv-python>=4.8.0
numpy>=1.24.0

# Optional: DeepSORT (heavier, requires appearance model)
# deep-sort-realtime>=1.3.2

# Phase 4: Response Layer (API)
fastapi>=0.104.0
uvicorn>=0.24.0
python-multipart>=0.0.9

# Database (PostgreSQL)
sqlalchemy>=2.0.0
psycopg2-binary>=2.9.9
alembic>=1.13.0

# Caching (Redis)
redis>=5.0.0

# AI/LLM (Google Gemini via HTTP)
requests>=2.31.0
yt-dlp>=2024.8.6

# Sprint 1: Security & Testing
python-dotenv>=1.0.0
slowapi>=0.1.9
pytest>=7.4.0
pytest-asyncio>=0.21.0
httpx>=0.25.0

# Phase 6: Edge/Cloud Hybrid Intelligence
# Edge layer (always installed)
requests>=2.31.0

# Optional: GPU Acceleration (uncomment if using CUDA)
# torch>=2.0.0
# torchvision>=0.15.0

# Optional: Cloud Layer - AWS GPU Models (install on EC2 only)
# transformers>=4.30.0     # CLIP
# timm>=0.9.0              # MiDaS backbone
# mediapipe>=0.10.0        # Pose estimation
# pytorchvideo>=0.1.5      # SlowFast action recognition

